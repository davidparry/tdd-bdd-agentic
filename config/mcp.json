{
  "mcpServers": {
    "tdd-workflow": {
      "command": "java",
      "args": [
        "-Dworkshop.root=${workspaceFolder}",
        "-jar",
        "${workspaceFolder}/mcp-server/target/tdd-mcp-server.jar"
      ]
    }
  }
}
